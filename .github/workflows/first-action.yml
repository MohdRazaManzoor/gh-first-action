name: workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print hello
        run: echo "hello world"
      - name: print bye
        run: echo "good bye"
      - name: pipeline
        run: |
          echo "first job"
          echo "second job"
